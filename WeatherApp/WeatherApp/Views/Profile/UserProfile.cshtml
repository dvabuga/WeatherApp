@using WeatherApp.DB;
@model List<UserCity>


@* использовать попап окно как универсальный компонент, так как он используется в нескольких страницах/
  либо не использовать тут попап вообще *@

@{
  if (Model.Count() == 0)
  {
    <div style="display:flex; flex-direction:column; align-items:center;">
      <div>
        <h2>Вы не подписались не на один город!</h2>
      </div>
      <div>
        <h2>
          <a style="border-bottom:dashed; cursor:default">Подписаться?</a>
        </h2>
      </div>
    </div>
  }



}
